# Previews

Thumbnails are catalog/QA assets, not runtime substitutes for full-resolution
content. Use a consistent 16:9 or square crop, WebP/PNG, and a predictable
filename (`<id>-preview.webp`). Every manifest entry should point to one.
